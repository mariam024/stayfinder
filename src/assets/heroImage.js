/**
 * Homepage hero only — not used in Explore STATIC_IMAGES or card fallbacks.
 * Bundled via Vite so the image always resolves in dev and production builds.
 */
import heroHome from "./hero-home.jpg";

export const HERO_IMAGE_URL = heroHome;
